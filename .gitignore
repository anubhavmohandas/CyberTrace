# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv/

# Environment variables
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Project specific
data/
*.db
*.sqlite
# Any file actually holding keys. The tracked cybertrace/api_key_registry.py is
# only a loader — it reads os.getenv and stores nothing.
api_keys.py

# OSINT output files. Scratch run output stays out of the repo.
# evidence/ is the deliberate exception and IS tracked: the saved --save JSON
# there is the corpus the extractor-precision audit measures against, so it has
# to be versioned alongside the normalizers whose behaviour it pins down.
# Everything in evidence/ is published — read a run before adding it.
output/
outputs/
results/
reports/
*.json.out
*.json
# !evidence/*.json
*.report

# Sensitive files — extra safety net
*.key
*.pem
*.p12
secrets.yml
secrets.yaml
config.local.py

# Tor
*.onion_cache
